## 작업 변경 원칙

* 사용자의 새 요청을 기존 계획 전체를 재설계하라는 의미로 해석하지 않는다.
* 이미 확정된 역할, 트랙, 구현 순서, 기능 소유권은 명시적인 변경 요청이 없는 한 유지한다.
* 특정 문구나 항목 수정 요청은 해당 범위만 최소 수정한다.
* 난이도나 효율을 이유로 기능 담당을 임의로 옮기지 않는다.
* 작업량 불균형은 기능 이전보다 페어링, 공동 설계, 코드 리뷰로 먼저 해결한다.

### 현재 확정된 구현 방식

* Admin 트랙과 Kiosk 트랙은 병렬 진행한다.
* 각 트랙 내부에서는 수직 슬라이스 방식으로 구현한다.
* Admin 트랙은 Admin 구현을 계속 진행한다.
* Kiosk 트랙은 Kiosk 구현을 계속 진행한다.
* 주문 생성 핵심 로직은 공동 페어링한다.
* `본인`, `친구` 같은 개인 지칭은 문서에 사용하지 않는다.

### 중요

사용자가 한 부분에 대해 피드백했다고 해서 연관된 전체 구조까지 연쇄적으로 변경하지 않는다.

수정 전 반드시 확인:
"지금 바꾸려는 내용이 사용자가 실제로 요청한 범위를 넘어가고 있는가?"

넘어간다면 수정하지 않는다.

요청받지 않은 개선은 제안만 할 수 있으며, 문서나 코드에 실제 반영하지 않는다.