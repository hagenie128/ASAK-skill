interface:
  display_name: "문서 동기화"
  short_description: "현재 코드 근거로 ASAK 문서와 계약을 안전하게 갱신"
  default_prompt: "Use $asak-doc-sync to compare the current ASAK code with the specified documents and update only approved documentation."

policy:
  allow_implicit_invocation: true
